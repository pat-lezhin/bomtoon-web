// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://pat-lezhin.github.io',
  base: '/bomtoon-web', // ← 레포 이름과 동일
});
// This configuration sets the base path for the Astro project to match the GitHub Pages repository name.  